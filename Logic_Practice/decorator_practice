def my_decorator(my_function):
    def wrapper():
        print("Wrap it in beginning of {my_function}")
        my_function()
        print("Wrap it in the end of {my_function}")

@my_decorator
def greeting():
    print("Say Hello")